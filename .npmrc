# expo-router pulls in react-dom for its (unused here) web target, and that
# react-dom release peer-requires a newer react than the Expo SDK 57 template
# pins. Strict peer resolution fails on that skew for every subsequent install.
legacy-peer-deps=true
